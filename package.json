{
  "name": "bedrock-battle-royale",
  "version": "0.1.0",
  "private": true,
  "description": "Addon de Battle Royale para Minecraft Bedrock (Script API)",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "package": "npm run build && node tools/package.mjs"
  },
  "devDependencies": {
    "@minecraft/server": "2.0.0",
    "@minecraft/server-ui": "2.0.0",
    "adm-zip": "^0.6.1",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
